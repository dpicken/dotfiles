#/!bin/sh

if [ "$1" = "--help" ]; then
  echo "Usage: $0 [--help] [<tagfile>]"
  exit
fi

tagfile_arg=${1:+-f $1}

# ctags doesn't like quoted file names
sfind | sed 's/^\"\(.*\)\"$/\1/' | ctags --extra=+q -L - $tagfile_arg
